@model List<CategoryViewModel>

<div class="card shadow border-0 mt-4">
    <div class="card-header bg-secondary bg-gradient ml-0 py-3">
        <div class="row">
            <div class="col-12 text-center">
                <h2 class="text-white py-2">Category List</h2>
            </div>
        </div>
    </div>
    <div class="card-body p-4">
        <div class="row pb-3">
            <div class="col-6">
            </div>
            <div class="col-6 text-end">
                <a asp-controller="Category" asp-action="Add" class="btn btn-primary">
                    <i class="bi bi-plus-circle"></i>  Create New Category
                </a>
            </div>
        </div>

        <table id="categoryTbl" class="table table-bordered table-striped" style="width:100%">
            <thead>
                <tr>
                    <th>
                        Category Name
                    </th>
                    <th>
                        Display Order
                    </th>
                    <th>
                        Is Active?
                    </th>
                    <th>Create Date</th>
                    <th>Update Date</th>
                    <th></th>
                </tr>
            </thead>
          @*   <tbody>
                @foreach (var categoryViewModel in Model.OrderBy(u => u.DisplayOrder))
                {
                    <tr>
                        <td>@categoryViewModel.Name</td>
                        <td>
                            @categoryViewModel.DisplayOrder
                        </td>
                        <td>
                            @categoryViewModel.IsActive
                        </td>
                        <td>
                            @categoryViewModel.CreateDate
                        </td>
                        <td>
                            @categoryViewModel.UpdateDate
                        </td>
                        <td>
                            <div class="w-75 btn-group" role="group">
                                <a asp-controller="Category" asp-action="Update" asp-route-id="@categoryViewModel.Id" class="btn btn-primary mx-2">
                                    <i class="bi bi-pencil-square"></i> Edit
                                </a>
                                <a asp-controller="Category" asp-action="Delete" asp-route-id="@categoryViewModel.Id" class="btn btn-danger mx-2">
                                    <i class="bi bi-trash-fill"></i> Delete
                                </a>
                            </div>
                        </td>
                    </tr>
                }
            </tbody> *@
        </table>

    </div>
</div>


@section Scripts {
    <script src="~/js/category.js"></script>
}